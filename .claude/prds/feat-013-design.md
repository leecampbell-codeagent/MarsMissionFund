# Design Spec: feat-013 — KYC Verification (Mock Provider)

> Component-level UI specification. Generated by Design Speccer.
> Feature spec: feat-013-spec.md
> Design system: specs/standards/brand.md

---

## Page Layouts

### KYC Verification Page

**Route:** `/settings/verification`
**Layout:** Single-column, centred content (matching the existing settings pages pattern)
**Max width:** 600px centred (consistent with `settings-profile.tsx` and `settings-preferences.tsx`)

#### Layout Structure

```
┌─────────────────────────────────────────────────────────────┐
│ Nav bar (existing — no changes)                             │
├─────────────────────────────────────────────────────────────┤
│                  [page background: --color-bg-page]         │
│                                                             │
│  ┌──────────────────────── 600px ─────────────────────────┐ │
│  │  01 — IDENTITY VERIFICATION   [section label]          │ │
│  │  VERIFY YOUR IDENTITY         [page heading]           │ │
│  │                                                        │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  KYC Status Banner                               │  │ │
│  │  │  [status badge + status description text]        │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │                                                        │ │
│  │  [Submission Form — shown conditionally]               │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  Document Type Selector                          │  │ │
│  │  │  Front Document Upload                           │  │ │
│  │  │  Back Document Upload (conditional)              │  │ │
│  │  │  Submit Button (primary CTA)                     │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Responsive behaviour:**
- Desktop (≥1024px): Centred 600px column with 48px vertical padding
- Tablet (768–1023px): Same 600px column with 32px vertical padding
- Mobile (<768px): Full-width with 24px horizontal padding, 24px vertical padding

---

## Components

### Component: `KycStatusBanner`

**File:** `packages/frontend/src/components/kyc/kyc-status-banner.tsx`
**Reuses:** Badge pattern from `specs/standards/brand.md` Section 3.5. New component.

#### Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `status` | `KycStatus` | Yes | — | Current KYC status string |
| `failureCount` | `number` | No | `0` | Current failure count (shown for pending_resubmission and rejected) |
| `verifiedAt` | `string \| null` | No | `null` | ISO 8601 string (shown when verified) |

#### Visual Specification

**Container:**
- Background: varies by status (see Status Badges section)
- Border radius: `--radius-input` (12px) — slightly tighter than a card, matching inline alert patterns
- Padding: 16px 20px
- Margin bottom: 32px

**Layout inside banner:**
- Row: `[dot indicator — 8px] [gap 10px] [status text]`
- For pending_resubmission, rejected, locked: second line showing failure count in `--type-body-small` and `--color-text-tertiary`

**Typography:**
- Status label: `--type-button` (DM Sans 600, 14px), left-aligned
- Sub-text: `--type-body-small` (DM Sans 400, 13px), `--color-text-secondary`, margin-top 4px

#### States by KYC Status

**`not_verified`:**
- Background: `rgba(var(--afterburn), 0.08)` — resolves to `--color-status-warning` at 8% opacity
- Border: `1px solid` `--color-status-warning` at 20% opacity
- Dot: `--color-status-warning` (afterburn)
- Label: "Identity not verified"
- Sub-text: "Complete verification to unlock Creator features."

**`pending`:**
- Background: `--color-status-new-bg` (orbit at 40%)
- Border: `1px solid` `--color-status-new-border`
- Dot: `--color-status-new` (signal-blue), with pulsing animation
- Label: "Verification in progress"
- Sub-text: "Your documents are being reviewed. This usually takes a few seconds."

**`pending_resubmission`:**
- Background: `rgba(var(--afterburn), 0.08)`
- Border: `1px solid` `--color-status-warning` at 20% opacity
- Dot: `--color-status-warning`
- Label: "Resubmission required"
- Sub-text: "Please upload your documents again." (if failureCount > 1, append " Attempt {failureCount} of 5.")

**`in_manual_review`:**
- Background: `--color-status-new-bg`
- Border: `1px solid` `--color-status-new-border`
- Dot: `--color-status-new`
- Label: "Under manual review"
- Sub-text: "A team member is reviewing your submission. This may take up to 24 hours."

**`verified`:**
- Background: `--color-status-success-bg`
- Border: `1px solid` `--color-status-success-border`
- Dot: `--color-status-success`
- Label: "Identity verified"
- Sub-text: If `verifiedAt` is provided: "Verified on {formatted date}" in `--color-text-tertiary`

**`rejected`:**
- Background: `rgba(var(--red-planet), 0.08)` — equivalent to `--color-status-error` at 8% opacity
- Border: `1px solid` `--color-status-error` at 20% opacity
- Dot: `--color-status-error`
- Label: "Verification failed"
- Sub-text: "Your verification was rejected. Please resubmit your documents." (if failureCount > 0, append " Attempt {failureCount} of 5.")

**`expired`:**
- Background: `rgba(var(--afterburn), 0.08)`
- Border: `1px solid` `--color-status-warning` at 20% opacity
- Dot: `--color-status-warning`
- Label: "Verification expired"
- Sub-text: "Your identity verification has expired. Please resubmit to continue using Creator features."

**`reverification_required`:**
- Background: `rgba(var(--afterburn), 0.08)`
- Border: `1px solid` `--color-status-warning` at 20% opacity
- Dot: `--color-status-warning`
- Label: "Re-verification required"
- Sub-text: "Please re-verify your identity to continue using Creator features."

**`locked`:**
- Background: `rgba(var(--red-planet), 0.08)`
- Border: `1px solid` `--color-status-error` at 20% opacity
- Dot: `--color-status-error`
- Label: "Account locked"
- Sub-text: "Your account has been locked for KYC submissions after repeated failures. Please contact support."

#### Accessibility

- **Role:** `status` (aria-live region, polite) — allows screen reader to announce status changes
- **Dot indicator:** `aria-hidden="true"` — decorative
- **Announced text:** Screen reader reads the full label + sub-text when status changes

---

### Component: `DocumentTypeSelector`

**File:** `packages/frontend/src/components/kyc/document-type-selector.tsx`
**Reuses:** Form input pattern from `specs/standards/brand.md` Section 3.6. New component.

#### Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `value` | `KycDocumentType \| ''` | Yes | — | Selected document type |
| `onChange` | `(value: KycDocumentType) => void` | Yes | — | Selection handler |
| `disabled` | `boolean` | No | `false` | Disables selection |

#### Visual Specification

**Container:** Renders as a group of 3 radio-style selector cards.

**Individual selector card:**
- Background (unselected): `--color-bg-input`
- Background (selected): `--color-bg-elevated`
- Border (unselected): `1px solid` `--color-border-input`
- Border (selected): `1px solid` `--color-action-primary`
- Border radius: `--radius-input` (12px)
- Padding: 14px 16px
- Cursor: pointer
- Display: flex, align-items center, gap 12px

**Radio dot (custom):**
- Outer circle: 18px, border `2px solid` `--color-border-input` (unselected) or `--color-action-primary` (selected)
- Inner dot (selected only): 10px circle, `--color-action-primary` fill
- flex-shrink: 0

**Document type label:**
- Font: `--type-body` (DM Sans 400, 16px), `--color-text-primary`

**Document sub-label** (description below document type):
- Passport → "Single photo page required"
- National ID → "Front and back required"
- Driver's Licence → "Front and back required"
- Font: `--type-body-small` (DM Sans 400, 13px), `--color-text-tertiary`

**Layout:** Cards stacked vertically with 8px gap between cards.

**Group label above:** "DOCUMENT TYPE" in `--type-input-label` (Space Mono 600, 12px uppercase, letter-spacing 0.05em), `--color-text-tertiary`, margin-bottom 8px.

#### States

**Default (no selection):** All three cards shown with `--color-border-input` border.

**Selected:** Selected card shows `--color-action-primary` border + inner filled dot.

**Focus-visible:** `outline: 2px solid var(--color-action-primary-hover); outline-offset: 2px`

**Disabled:** All cards at 40% opacity, cursor `not-allowed`.

#### Accessibility

- Each card is `<label>` wrapping a visually hidden `<input type="radio">` with a shared `name` attribute
- `<fieldset>` wraps the group with `<legend class="form-label">DOCUMENT TYPE</legend>`
- Keyboard: tab to focus group, arrow keys to navigate between options
- Screen reader: "Passport, single photo page required, radio button, 1 of 3"

---

### Component: `DocumentUploadField`

**File:** `packages/frontend/src/components/kyc/document-upload-field.tsx`
**Reuses:** Form input pattern and error pattern from settings-profile.tsx. New component.

#### Props

| Prop | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| `id` | `string` | Yes | — | Field ID for label association |
| `label` | `string` | Yes | — | e.g. "Front of Document", "Back of Document" |
| `accept` | `string` | No | `"image/jpeg,image/png,application/pdf"` | MIME types |
| `maxBytes` | `number` | No | `20971520` | 20 MB in bytes |
| `onFileChange` | `(file: File \| null) => void` | Yes | — | File selection handler |
| `error` | `string \| null` | No | `null` | Inline error message |
| `disabled` | `boolean` | No | `false` | Disables the field |

#### Visual Specification

**Container:**
- Margin bottom: 24px (matching form-field spacing in settings-profile.tsx)

**Label:**
- `--type-input-label` (Space Mono 600, 12px uppercase, 0.05em letter-spacing)
- `--color-text-tertiary`
- Margin bottom: 8px

**Upload area** (dashed border zone — replaces standard file input):
- Background: `--color-bg-input`
- Border: `1.5px dashed` `--color-border-input`
- Border radius: `--radius-input` (12px)
- Padding: 20px 16px
- Display: flex, flex-direction column, align-items center, gap 8px
- Cursor: pointer
- Transition: border-color `--motion-hover-duration` `--motion-hover-easing`

**Icon:** Upload icon (SVG, 24px) in `--color-text-tertiary`. Decorative, `aria-hidden="true"`.

**Upload prompt text:**
- "Click to upload" — `--type-body-small` (DM Sans 400, 13px), `--color-text-secondary`
- "or drag and drop" — `--type-body-small`, `--color-text-tertiary`

**Format hint:**
- "JPEG, PNG or PDF — max 20 MB"
- `--type-label` (Space Mono 400, 11px uppercase), `--color-text-tertiary`
- Margin top: 4px

**Selected file state** (replaces the prompt when a file is chosen):
- Background: `--color-bg-elevated`
- Border: `1.5px solid` `--color-border-emphasis` (orbit)
- Display: flex, align-items center, gap 12px
- File icon (SVG, 20px) in `--color-status-success`
- Filename: `--type-body-small`, `--color-text-primary`
- File size: `--type-label`, `--color-text-tertiary`
- Remove button: "×" (×), `--color-text-tertiary`, hover `--color-text-error`, float right

**Hover state (unselected):**
- Border: `1.5px dashed` `--color-border-emphasis`

**Focus-visible state:**
- `outline: 2px solid var(--color-action-primary-hover); outline-offset: 2px`

**Error state:**
- Border: `1.5px dashed` `--color-status-error`
- Error message below: `--type-body-small` (13px), `--color-text-error`, margin-top 6px, matching `form-error` class pattern from settings-profile.tsx

**Disabled state:**
- 40% opacity, cursor `not-allowed`, border `--color-border-input`

#### Accessibility

- `<label>` wraps the upload area with `htmlFor` matching a visually hidden `<input type="file">`
- The upload area has `role="button"` and `tabindex="0"` for keyboard activation
- Keyboard: Enter or Space activates file picker
- When a file is selected, announce via `aria-live="polite"`: "{filename} selected, {size}"
- Error message linked via `aria-describedby`

---

### Component: `KycVerificationPage` (page component)

**File:** `packages/frontend/src/pages/kyc-verification.tsx`
**Reuses:** Layout pattern from `settings-profile.tsx`. New page component.

#### Props

None (page component, data from hooks).

#### Visual Specification

**Page container:**
- `display: flex; justify-content: center; flex: 1; padding: 24px 0` (matching settings pages)
- Responsive padding: 32px 0 at ≥768px, 48px 0 at ≥1024px

**Content container:**
- `max-width: 600px; width: 100%`

**Section label:**
- "01 — IDENTITY VERIFICATION"
- `--type-section-label` (Space Mono 400, 11px, letter-spacing 0.3em, uppercase)
- `--color-text-accent` (launchfire)
- Margin bottom: 16px

**Page heading:**
- "VERIFY YOUR IDENTITY"
- `--type-section-heading` (Bebas Neue, 40px, letter-spacing 0.04em, uppercase)
- `--color-text-primary`
- Line-height: 1
- Margin bottom: 32px
- Responsive: 56px at ≥640px

**Submit error message** (shown on submission failure):
- Same pattern as `settings-page__save-error` in settings-profile.tsx
- Background: `rgba(var(--red-planet), 0.1)`
- Border: `1px solid` `--color-status-error`
- Border radius: `--radius-input`
- Padding: 12px 16px
- Font: `--type-body-small` (DM Sans 400, 13px), `--color-text-error`
- Margin bottom: 24px
- `role="alert"`

**Submission form structure** (shown when status permits submission):
1. `DocumentTypeSelector` — select document type
2. `DocumentUploadField` for front document (always shown when form is shown)
3. `DocumentUploadField` for back document (shown only when document type is `national_id` or `drivers_licence`)
4. Submit button row

**Submit button:**
- Label: "Submit for Verification"
- Variant: Primary (background `--gradient-action-primary`, text `--color-action-primary-text`, shadow `--color-action-primary-shadow`)
- `--radius-button` (full-radius pill)
- `--type-button` (DM Sans 600, 14px, letter-spacing 0.01em)
- Padding: 12px 32px
- `display: flex; justify-content: flex-end` (right-aligned, same as settings save button)
- Disabled: when no document type selected, or front document not selected (or back required but not selected), or mutation pending
- Pending state: show spinner + "Submitting..." label (same spinner pattern as settings-profile.tsx `btn-spinner`)

**Post-submit pending state** (when status is `pending` or `in_manual_review`):
- Show `KycStatusBanner` only (no form)
- Below the banner, a supplementary message card:
  - Background: `--color-bg-surface`
  - Border: `1px solid` `--color-border-subtle`
  - Border radius: `--radius-card`
  - Padding: 24px
  - Heading (if shown): omit — status banner is sufficient
  - Body text: "We're verifying your identity. You'll be able to create campaigns once verification is complete."
  - Font: `--type-body` (DM Sans 400, 16px), `--color-text-secondary`

**Post-verification success state** (when status is `verified`):
- Show `KycStatusBanner` with `verified` styling
- Below: success message card (same surface card pattern as pending state)
  - Body text: "Your identity is verified. You can now create and submit campaigns."
  - A secondary-variant button: "Start a Campaign →" (links to `/campaigns/new` when that route exists — for now render as disabled secondary button with `href="#"`)

#### Loading State

Full-page skeleton matching settings-profile.tsx skeleton pattern:
```
┌──────────────────────────────────────────────────┐
│ [skeleton 120px × 16px]  (section label)          │
│ [skeleton 280px × 48px]  (heading)                │
│ [skeleton 100% × 56px]   (status banner)          │
│ [skeleton 100% × 48px]   (document type)          │
│ [skeleton 100% × 48px]   (document type)          │
│ [skeleton 100% × 48px]   (document type)          │
│ [skeleton 100% × 80px]   (upload field)           │
│ [skeleton 100% × 80px]   (upload field)           │
│ [skeleton 160px × 44px, float right] (button)     │
└──────────────────────────────────────────────────┘
```

Skeleton elements:
- Background: `--color-bg-input`
- Border radius: `--radius-input`
- Animation: `skel-pulse` keyframes (same as settings-profile.tsx)
- `prefers-reduced-motion`: animation: none, opacity 0.7

---

## Design Tokens

| Semantic Token | Usage in this feature |
|---|---|
| `--color-bg-page` | Page background |
| `--color-bg-surface` | Info/message card backgrounds |
| `--color-bg-elevated` | Selected document type card background; selected upload field background |
| `--color-bg-input` | Unselected document type cards; upload area background; skeleton elements |
| `--color-bg-overlay` | Not used |
| `--color-text-primary` | Page heading, document type labels, selected file names |
| `--color-text-secondary` | Banner sub-text, body copy in message cards, upload prompt text |
| `--color-text-tertiary` | Section label sub-text, file size, format hints, field labels |
| `--color-text-accent` | Section label "01 — IDENTITY VERIFICATION" |
| `--color-text-success` | Verified state label |
| `--color-text-error` | Error messages, locked/rejected label |
| `--color-text-warning` | Not-verified / pending-resubmission label |
| `--color-action-primary` | Selected radio border, selected upload field border |
| `--color-action-primary-hover` | Focus outlines |
| `--color-action-primary-text` | Submit button text |
| `--color-action-primary-shadow` | Submit button shadow |
| `--color-border-input` | Unselected radio border; default upload area border |
| `--color-border-emphasis` | Hover upload area border; selected upload field border |
| `--color-border-subtle` | Message card borders |
| `--color-border-accent` | Not used |
| `--color-status-success` | Verified badge dot; selected file icon |
| `--color-status-success-bg` | Verified banner background |
| `--color-status-success-border` | Verified banner border |
| `--color-status-error` | Rejected/locked banner dot; error messages; error upload border |
| `--color-status-warning` | Not-verified/pending-resubmission/expired/reverification banner dot |
| `--color-status-new` | Pending/in-manual-review banner dot |
| `--color-status-new-bg` | Pending/in-manual-review banner background |
| `--color-status-new-border` | Pending/in-manual-review banner border |
| `--gradient-action-primary` | Submit button background |
| `--radius-button` | Submit button border radius |
| `--radius-input` | Upload area, selector cards, banner, form error |
| `--radius-card` | Message card |
| `--type-section-label` | "01 — IDENTITY VERIFICATION" |
| `--type-section-heading` | "VERIFY YOUR IDENTITY" |
| `--type-button` | Submit button label, status label in banner |
| `--type-body` | Message card body text, upload prompt text |
| `--type-body-small` | Banner sub-text, file size, error messages, file name |
| `--type-input-label` | Form field labels |
| `--type-label` | Format hints in upload area |
| `--motion-hover-duration` | Hover transitions on cards and upload area |
| `--motion-hover-easing` | Hover transitions |
| `--motion-enter-duration` | Status banner appearance animation |
| `--motion-enter-easing` | Status banner appearance animation |

---

## Status Badges

KYC status is communicated via the `KycStatusBanner` component (inline banner, not a pill badge). The banner uses the status-indicator dot + text pattern from `specs/standards/brand.md` Section 3.5, adapted to a wider informational layout.

| KYC Status | Banner Dot Colour | Label | Background / Border |
|---|---|---|---|
| `not_verified` | `--color-status-warning` | "Identity not verified" | warning-tinted surface |
| `pending` | `--color-status-new` (pulse) | "Verification in progress" | `--color-status-new-bg` / `--color-status-new-border` |
| `pending_resubmission` | `--color-status-warning` | "Resubmission required" | warning-tinted surface |
| `in_manual_review` | `--color-status-new` | "Under manual review" | `--color-status-new-bg` / `--color-status-new-border` |
| `verified` | `--color-status-success` | "Identity verified" | `--color-status-success-bg` / `--color-status-success-border` |
| `expired` | `--color-status-warning` | "Verification expired" | warning-tinted surface |
| `reverification_required` | `--color-status-warning` | "Re-verification required" | warning-tinted surface |
| `rejected` | `--color-status-error` | "Verification failed" | error-tinted surface |
| `locked` | `--color-status-error` | "Account locked" | error-tinted surface |

**Warning-tinted surface**: background `rgba(var(--afterburn), 0.08)`, border `1px solid rgba(var(--afterburn), 0.2)`
**Error-tinted surface**: background `rgba(var(--red-planet), 0.08)`, border `1px solid rgba(var(--red-planet), 0.2)`

Note: The `not_verified` default state uses warning tinting to draw attention without being alarming — matching the "action needed" convention used for incomplete states in settings pages.

---

## Responsive Behaviour

| Breakpoint | Width | Layout Changes |
|------------|-------|---------------|
| Desktop | ≥1024px | Centred 600px column, 48px vertical padding, full-width form within content area |
| Tablet | 768–1023px | Centred 600px column (if viewport allows) or full-width with 24px horizontal padding, 32px vertical padding |
| Mobile | <768px | Full-width with 24px horizontal padding, 24px vertical padding; all form elements full-width |

**Component-specific responsive notes:**
- `DocumentTypeSelector`: Cards are full-width at all breakpoints (no horizontal scroll — cards are already stacked vertically)
- `DocumentUploadField`: Full-width at all breakpoints; the upload zone height reduces from 100px to 80px on mobile
- Submit button: Right-aligned on desktop/tablet; full-width on mobile
- Page heading: 40px on mobile, 56px on desktop (≥640px)

---

## Animation & Transitions

| Element | Trigger | Animation | Duration | Easing |
|---------|---------|-----------|----------|--------|
| `KycStatusBanner` (initial render) | Mount | Slide down + fade in | `--motion-enter-duration` (300ms) | `--motion-enter-easing` |
| `KycStatusBanner` (status change) | Status prop change | Fade out → fade in | `--motion-hover-duration` (150ms) | `--motion-hover-easing` |
| Document type selector card | Hover (unselected) | Border colour → `--color-border-emphasis` | `--motion-hover-duration` | `--motion-hover-easing` |
| Document type selector card | Selected | Border colour → `--color-action-primary`, inner dot appears | `--motion-hover-duration` | `--motion-hover-easing` |
| Upload area | Hover | Border colour → `--color-border-emphasis` | `--motion-hover-duration` | `--motion-hover-easing` |
| Upload area | File selected | Zone content changes to file preview | `--motion-hover-duration` | `--motion-hover-easing` |
| Pending dot indicator | `pending`/`in_manual_review` | Pulse scale 1.0→1.2→1.0 | 1.5s | ease-in-out, infinite |
| Submit button spinner | isPending | Rotate 360° | 800ms | linear, infinite |
| Success state appearance | Status → `verified` | Fade in with slight upward translate | `--motion-enter-duration` | `--motion-enter-easing` |
| Skeleton loading | Mount | Pulse opacity 0.5→1.0 | 1.5s | ease-in-out, infinite |

**`prefers-reduced-motion: reduce` behaviour:**
- All slide/translate animations → instant appearance (no transform)
- Fade animations → instant appearance (no opacity transition)
- Pending dot pulse → static dot, no animation
- Submit spinner → no rotation, opacity 0.5
- Skeleton → no animation, opacity 0.7 (static)

---

## New Patterns Introduced

### Status Banner Pattern

The `KycStatusBanner` introduces a new "inline status banner" pattern distinct from the existing badge pill pattern. This pattern should be considered for reuse wherever a larger, descriptive status message is needed (as opposed to a compact pill badge for list/card views).

**Pattern characteristics:**
- Inline in the content flow (not floating or overlapping)
- 16px × 20px horizontal padding
- Status dot (8px) + label text + optional secondary line
- Colour-coded background tints derived from status semantic tokens
- `role="status"` with `aria-live="polite"` for dynamic updates

This pattern may be extracted to `packages/frontend/src/components/ui/status-banner.tsx` if other features need it.

---

## Accessibility Checklist

- [x] All interactive elements are keyboard accessible (document type selector uses native radio inputs; upload area has `tabindex="0"` and keyboard activation; submit button is a native `<button>`)
- [x] All icons have `aria-hidden="true"` (decorative); upload area has accessible label text
- [x] Colour is never the sole indicator — every status uses both a colour dot AND a text label
- [x] Focus states are visible on all interactive elements (2px `--color-action-primary-hover` outline with 2px offset, consistent with brand spec Section 5.3)
- [x] Screen reader announces dynamic content changes via `aria-live="polite"` on the status banner and `role="alert"` on error messages
- [x] Contrast ratio: all text uses semantic token pairings that meet WCAG AA. Specifically: `--color-text-primary` on `--color-bg-page` (14.8:1, AAA); `--color-text-secondary` on `--color-bg-page` (10.7:1, AA); `--color-text-tertiary` on `--color-bg-page` (5.4:1, AA — metadata only)
- [x] `--color-text-accent` (launchfire) used only for the section label at 11px uppercase — this is large-text weight equivalent at 0.3em spacing, acceptable per brand spec Section 5.1 note
- [x] Form labels use `<label>` elements with proper `htmlFor` / `id` association
- [x] File upload uses a visually hidden native `<input type="file">` for browser accessibility support; the custom upload zone is a labelled `<button>` alternative
- [x] `<fieldset>` with `<legend>` used for the document type radio group
- [x] Loading skeletons include no interactive elements (screen readers skip them)
