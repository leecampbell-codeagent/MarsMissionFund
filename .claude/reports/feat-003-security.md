# Security Review: feat-003 — Campaign Creation, Submission, and Review Pipeline

> Security review results. Generated by Security Reviewer.
> Reviewed: 2026-03-05
> Reviewer: Security Reviewer Agent (Claude Sonnet 4.6)

## Verdict: 0 CRITICAL / 1 HIGH / 4 MEDIUM FINDINGS

The HIGH finding is the missing body validation on `POST /campaigns/:id/claim`, `POST /campaigns/:id/launch`, and `POST /campaigns/:id/archive`, which accept arbitrary request bodies with no rejection. All other fundamentals are sound. No merge-blocking critical issues found.

---

## Findings

### Critical (Must Fix Before Merge)

None.

---

### High (Must Fix Before Merge)

#### HIGH-001: `POST /:id/claim`, `POST /:id/launch`, `POST /:id/archive` Accept Arbitrary Request Bodies

- **Location:** `packages/backend/src/campaign/api/campaign-router.ts` lines 175–185, 264–274, 280–290
- **Issue:** Three state-transition endpoints do not parse or validate the request body at all. They accept any JSON payload without rejection. The `emptyBodySchema` pattern (correctly applied to `POST /:id/submit`) is not applied here. An attacker can send `{"status": "live", "reviewedByUserId": "..."}` or any other payload. While the current application code does not read these fields from the body (it uses the auth context and parameterised repository calls), this is a defence-in-depth failure. If a future refactor inadvertently reads from `req.body`, or if body-parsing middleware behaviour changes, injected fields could influence logic.
- **Recommendation:** Apply `z.object({}).strict().safeParse(req.body ?? {})` guards (identical to the pattern on `submit`) to all three endpoints before delegating to the app service.
- **Severity:** HIGH — spec explicitly requires no body acceptance on action-only endpoints; the missing guard is a design gap, not just a style issue.

---

### Medium (Should Fix)

#### MED-001: `reviewedByUserId` Partially Exposed in Review Queue Summary

- **Location:** `packages/backend/src/campaign/api/campaign-serializer.ts` lines 42–52
- **Issue:** `serializeCampaignSummary` (used for the review queue response) returns `creatorUserId` for every entry in the queue. All reviewers and admins can therefore enumerate MMF-internal creator UUIDs for every submitted campaign. The full `serializeCampaign` also returns `reviewedByUserId` (reviewer's internal UUID). While not high-severity (roles are already checked before the endpoint is reached), it exposes internal identifiers that could aid enumeration or correlation attacks. The spec does not require reviewer identity to be hidden from other reviewers, but the `creatorUserId` in the summary is superfluous.
- **Recommendation:** Remove `creatorUserId` from `serializeCampaignSummary` unless it is needed by a named client requirement. Consider returning only `id`, `title`, `category`, `fundingGoalCents`, `submittedAt`, and `status` in the summary.
- **Severity:** MEDIUM — data minimisation principle; no auth bypass.

#### MED-002: Dynamic SET Clause in `updateDraftFields` Uses Fixed `fieldMap` but No Runtime Key Validation

- **Location:** `packages/backend/src/campaign/adapters/pg-campaign-repository.adapter.ts` lines 190–252
- **Issue:** The `fieldMap` is a static object (defined at module scope in the function body) mapping camelCase schema keys to SQL column names. The loop iterates `Object.entries(fieldMap)` and then reads `(input as Record<string, unknown>)[camelKey]`. This design is safe because the field names used in SET clauses come exclusively from the trusted `fieldMap` values (snake_case column names hardcoded), never from user input. All parameter values are passed via `$n` placeholders. However, the dynamic `${setClauses.join(', ')}` string interpolation inside the SQL template literal could become unsafe if a future developer adds a `fieldMap` entry whose value is not a safe column name (e.g., contains SQL syntax). The current code is not exploitable, but the pattern is fragile.
- **Recommendation:** Add a comment clearly marking the `fieldMap` values as the trusted whitelist for SET clause column names. Optionally validate that every value in `fieldMap` matches a known safe pattern (e.g., `/^[a-z_]+$/`) at startup or test time.
- **Severity:** MEDIUM — not currently exploitable; fragile pattern.

#### MED-003: `assignCreatorRole` — KYC Check Performed After Account Status Check, But Not in `createDraft` / `submitCampaign` Path for `accountStatus`

- **Location:** `packages/backend/src/campaign/application/campaign-app-service.ts` lines 64–114 (`createDraft`)
- **Issue:** `createDraft` checks `user.accountStatus !== 'active'` and throws `AccountNotActiveError`. However, `submitCampaign` (lines 170–387) skips the `accountStatus` check entirely — it only checks `creator` role and KYC. A user who had an active account when they obtained the Creator role but whose account was subsequently suspended can still submit campaigns. The `accountStatus` guard in `createDraft` is not applied consistently throughout the workflow.
- **Recommendation:** Add `if (user.accountStatus !== 'active') throw new AccountNotActiveError()` as the first check in `submitCampaign`, `launchCampaign`, and any other creator-initiated state transitions, mirroring the pattern in `createDraft`.
- **Severity:** MEDIUM — allows suspended/deactivated accounts to advance campaigns already in draft.

#### MED-004: Reviewer Partial ID Leaked in Frontend UI

- **Location:** `packages/frontend/src/pages/campaign/campaign-detail-page.tsx` line 427
- **Issue:** When a campaign is `under_review` and the current reviewer is not the assigned reviewer and is not an admin, the frontend renders: `"Claimed by reviewer {campaign.reviewedByUserId.substring(0, 8)}..."`. This leaks the first 8 characters of the assigned reviewer's internal UUID to any other reviewer who views the campaign. While this does not enable a direct attack, it can be used to fingerprint/track internal user identities and confirm account existence.
- **Recommendation:** Do not render any portion of `reviewedByUserId` in the UI. Replace with a generic message such as "Claimed by another reviewer" or surface a display name via a separate, role-gated API call.
- **Severity:** MEDIUM — data exposure; no auth bypass.

---

### Low (Fix When Convenient)

#### LOW-001: No Rate Limiting on State-Transition Endpoints

- **Location:** All `POST /campaigns/:id/*` endpoints
- **Issue:** None of the campaign state-transition endpoints have explicit rate limiting. This is an infrastructure concern (typically handled at the API gateway or load balancer layer), but it is worth documenting. A reviewer or admin with valid credentials could rapidly spam the `claim` endpoint to exhaust the review queue, or a creator could trigger rapid `submit`/`archive` cycles.
- **Recommendation:** Apply per-user rate limiting at the infrastructure layer (e.g., API Gateway throttling, nginx `limit_req_zone`). No code change required in this PR — document the expectation.
- **Severity:** LOW — no implementation-level controls required in this PR.

#### LOW-002: `heroImageUrl` Validated as HTTPS URL but No Domain Allowlist

- **Location:** `packages/backend/src/campaign/api/schemas/update-campaign.schema.ts` line 46
- **Issue:** The schema validates that `heroImageUrl` starts with `https://` and is a valid URL (≤2048 chars), but does not restrict the domain. A creator could submit an image URL pointing to an arbitrary external host (including attacker-controlled infrastructure), which the frontend renders in an `<img>` tag. This could be used for pixel tracking, exfiltrating browser information, or serving malicious content if the CSP is permissive.
- **Recommendation:** Enforce a domain allowlist in the Zod schema (e.g., only permit URLs from trusted CDN domains such as `upload.cloudinary.com` or an S3 bucket). Alternatively, implement a server-side image proxy that fetches and re-hosts images.
- **Severity:** LOW in current context (no contributions/money movement yet); escalate to MEDIUM before launch.

#### LOW-003: `approveNotes` and `rejectionReason` Max Length Not Enforced at Schema Level

- **Location:** `packages/backend/src/campaign/api/schemas/approve-campaign.schema.ts` line 5; `packages/backend/src/campaign/api/schemas/reject-campaign.schema.ts` lines 5–6
- **Issue:** `reviewNotes`, `rejectionReason`, and `resubmissionGuidance` have no `max()` constraint in their Zod schemas. These fields are stored in `TEXT` columns (unbounded in PostgreSQL). A reviewer could submit megabytes of text, causing high memory allocation during JSON serialisation and potential DoS via response inflation.
- **Recommendation:** Add `.max(5000)` (or a similar sensible cap) to all three fields.
- **Severity:** LOW — requires a privileged role to exploit.

---

## Checklist Results

| # | Check | Result | Notes |
|---|-------|--------|-------|
| 1 | All endpoints require Clerk JWT | PASS | `requireAuth` middleware applied at `app.ts` mount; `requireAuthOrUnauthorized` used defensively inside each handler |
| 2 | Role checks enforced (Creator/Reviewer/Admin) | PASS | Creator role checked in `createDraft`, `submitCampaign`; Reviewer/Admin in `claimCampaign`, `approveCampaign`, `rejectCampaign`, `getReviewQueue`; Admin-only in `reassignReviewer` |
| 3 | `user_id` only from auth context | PASS | `clerkUserId` always from `getClerkAuth(req).userId`; internal UUID always resolved via DB lookup, never from request body |
| 4 | Zod validation on all request bodies | PARTIAL FAIL | `POST /:id/claim`, `POST /:id/launch`, `POST /:id/archive` missing body validation (HIGH-001) |
| 5 | String max lengths enforced | PARTIAL FAIL | `reviewNotes`, `rejectionReason`, `resubmissionGuidance` have no max (LOW-003); all other string fields have appropriate limits |
| 6 | Numeric ranges validated | PASS | `fundingBasisPoints`, `fundingGoalCents`, `fundingCapCents` validated via Zod regex and domain logic |
| 7 | UUID validation on path/body params | PASS | `reassignCampaignSchema` enforces `.uuid()` on `reviewerUserId`; campaign IDs are passed to DB as `$1` params |
| 8 | SQL injection: all queries parameterised | PASS | All queries use `$n` placeholders; dynamic SET clause in `updateDraftFields` uses a hardcoded column name whitelist (MED-002 documents fragility) |
| 9 | Monetary amounts as BIGINT in DB | PASS | `funding_goal_cents` and `funding_cap_cents` are `BIGINT` columns in migration |
| 10 | Monetary amounts serialised as strings in JSON | PASS | BIGINT returned as string by `pg` driver; serializer passes values through without casting |
| 11 | Error responses do not expose internals | PASS | All domain errors have safe static messages; stack traces not returned; 500 returns generic message |
| 12 | 404 used for "not owner" cases (not 403) | PASS | `CampaignNotFoundError` thrown when ownership check fails in `updateDraft`, `submitCampaign`, `launchCampaign`, `archiveCampaign` |
| 13 | Creators can only edit/archive their own campaigns | PASS | Ownership check `campaign.creatorUserId !== user.id` before every write operation |
| 14 | Reviewers can only claim submitted campaigns | PASS | `CampaignNotClaimableError` thrown if `campaign.status !== 'submitted'`; atomic optimistic lock via `WHERE status = $fromStatus` |
| 15 | Reviewers can only approve/reject their assigned campaign | PASS | `NotAssignedReviewerError` thrown if `campaign.reviewedByUserId !== user.id` (admin override permitted) |
| 16 | Admin-only actions enforced | PASS | `reassignReviewer` checks `isAdmin(user)`; archive by admin allowed via explicit branch |
| 17 | Rate limiting present | NOT PRESENT | Infrastructure concern; see LOW-001 |
| 18 | `GET /campaigns/review-queue` restricted | PASS | `ReviewerRoleRequiredError` thrown for non-reviewer/non-admin callers |
| 19 | `GET /campaigns/:id` access control (draft visibility) | PASS | Drafts only visible to creator and admin; reviewers cannot see drafts (EC-034 compliance); non-authenticated returns 401 |

---

## npm audit Summary

Run: `cd /workspace && npm audit 2>&1 | tail -20`

```
5 moderate severity vulnerabilities

esbuild <=0.24.2 (GHSA-67mh-4wv8-2f99)
  - esbuild dev server allows cross-origin requests
  - Affects: vite (0.11.0–6.1.6), vitest (0.3.3–3.0.0-beta.4), vite-node, @vitest/mocker
  - Fix: npm audit fix --force (installs vitest@4.x — breaking change)
```

**Assessment:** All 5 vulnerabilities are in dev tooling (`vite`, `vitest`, `esbuild`). None are present in production dependencies. The esbuild advisory affects the development server only — not the built application. No production risk. Upgrade of vitest is a separate maintenance task and is not required for this PR.

---

## Summary

feat-003 delivers a well-structured campaign pipeline with strong security foundations: consistent auth guards, proper ownership checks, optimistic locking on state transitions, SQL injection protection, correct monetary serialisation, and a thorough error-handler that maps all domain errors to safe HTTP responses without leaking internal details. The 404-for-ownership-checks pattern is correctly applied throughout.

The single HIGH finding (missing empty-body validation on three action endpoints) is a defence-in-depth gap that should be fixed before merge. Three MEDIUM findings address data minimisation, an inconsistent account-status guard across the creator workflow, and a minor reviewer identity leak in the frontend. Two LOW findings cover rate limiting (infra) and unbounded reviewer text fields.
