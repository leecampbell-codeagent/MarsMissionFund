# CI/CD Check: feat-013 — KYC Verification (Mock Provider)

> CI/CD pipeline review. Generated by CI/CD Checker.

## Verdict: PASS

## Summary

The existing CI/CD workflow at `.github/workflows/ci.yml` handles the KYC feature correctly without modification. The `MOCK_KYC` environment variable is documented in `.env.example` but is not required in CI because the test suite uses in-memory mock adapters directly (not via env var). The new migration `20260305000002_add_kyc_document_refs.sql` will be applied by the `dbmate up` step in CI. No new CI/CD configuration is required.

---

## Pipeline Analysis

### Jobs in CI workflow

| Job | Trigger | Status |
|-----|---------|--------|
| `lint-and-typecheck` | PR + push to main | Covers KYC files — no new linting rules needed |
| `unit-and-integration-tests` | PR + push to main | Runs `npm test` — KYC tests are included |
| `security-audit` | PR + push to main | Runs `npm audit --audit-level=high` — 0 vulnerabilities found |
| `build` | After all above pass | Compiles TypeScript + Vite — KYC code compiles cleanly |

### Migration Handling

The CI workflow installs `dbmate` and runs `dbmate up` before `npm test`:

```yaml
- name: Install dbmate
  run: |
    curl -fsSL -o /usr/local/bin/dbmate https://github.com/amacneil/dbmate/releases/latest/download/dbmate-linux-amd64
    chmod +x /usr/local/bin/dbmate
- name: Run migrations
  run: dbmate up
  env:
    DATABASE_URL: postgresql://test:test@localhost:5432/mmf_test?sslmode=disable
```

The new migration `20260305000002_add_kyc_document_refs.sql` follows the timestamp naming convention (`YYYYMMDDHHMMSS`) and has proper `-- migrate:up` / `-- migrate:down` sections with `BEGIN; ... COMMIT;` wrapping. It will be applied by `dbmate up` in the correct order.

Migration content adds `front_document_ref TEXT`, `back_document_ref TEXT`, and `submitted_at TIMESTAMPTZ` columns to `kyc_verifications` — consistent with what the `PgKycRepository` expects.

### Environment Variables

| Variable | Required in CI | Status |
|----------|---------------|--------|
| `DATABASE_URL` | Yes (for integration tests) | Already in CI workflow env |
| `MOCK_KYC` | No (tests use in-memory adapters directly) | Not needed in CI |
| `NODE_ENV` | Yes | Already in CI workflow env (`test`) |

**Explanation:** The test suite (`kyc-routes.test.ts`, `kyc-app-service.test.ts`) constructs `KycAppService` with `InMemoryKycRepository` and `MockKycAdapter` directly — not via `composition-root.ts`. The `MOCK_KYC` env var is only consumed by `composition-root.ts` for the live server, not during testing. Therefore, no new env vars need to be added to CI.

### No New Infrastructure Changes

No Terraform files were added or modified for feat-013. The KYC feature uses existing PostgreSQL infrastructure (the `kyc_verifications` table was created in a prior migration).

### Security Audit Step

`npm audit --audit-level=high` is configured. Running `npm audit` confirmed `0 vulnerabilities` — this step will pass.

---

## Potential Future CI Considerations

When the real Veriff KYC provider adapter is implemented (replacing the mock), CI will need:
- `VERIFF_API_KEY` in CI secrets (not in workflow env — use GitHub Secrets).
- A mock Veriff webhook server or the env var `MOCK_KYC=true` explicitly set to prevent real API calls during tests.

This is out of scope for feat-013 (mock-only implementation).
